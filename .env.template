# Optional OpenAI settings for bootstrap profile generation.
# The bootstrap path reads these automatically from .env when the variables
# are not already present in the shell environment.

OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1-mini
